# 시장 온도계 — 교차 검증 의뢰서

다른 모델·사람이 이 지표를 독립적으로 검증하기 위한 문서.
저장소: https://github.com/biguse/stock-radar

## 1차 외부 검증 결과 (2026-08-22, 반영 완료)

독립 재현 결과 7개 주장의 **숫자는 모두 일치**했으나, **해석 하나가 뒤집혔고**
사실관계 하나가 틀렸다. 아래는 반영 내역이다.

| 지적 | 판정 | 조치 |
|---|---|---|
| 워크포워드 57.3%의 기준선이 50%가 아니라 '항상 상승' 67.5% | **결론 뒤집힘** | 기준선 교체. 이 지표는 기준선보다 **10.2%p 낮다**고 화면에 명시 |
| 2026년 코스피 증권거래세 0.20%(0.05+농특세 0.15) | **사실 오류** | 0.15% → 0.20%. 하루 손익분기 61.0% → **63.3%**, 연간 비용 58% → **70.6%** |
| 손익분기 61%는 손익 크기를 고정한 조건부 값 | **과잉 일반화** | 손익비 민감도표 추가(41~76%). "무조건 진다" → "시장과 같은 손익 크기로 매일 전액을 굴리면 진다" |
| 구간 중앙값·손실확률의 신뢰구간이 매우 넓음 | 타당 | "정밀한 값으로 읽지 마세요" 명시 |
| `independentYears = n/252`는 유효 표본 수가 아님 | 타당 | "겹침의 크기를 가늠하는 값"으로 표현 정정 |
| 수출 데이터에 발표 지연 미반영(룩어헤드) | 타당 | 관측월 +1개월로 이동. 9,356일 갱신 |
| 온도가 안 쓰는 축의 결측이 날짜 전체를 버림 | 타당 | 온도 사용 축만 유효성을 좌우하도록 수정 |
| 연간 비용 58%는 단순합 | 타당 | 복리 기준 자본 감소율(50.7%) 병기 |
| 다중검정: p=0.116 → 본페로니 0.581 | 타당 | 결론 강화(증거 없음) |

**남은 두 과제도 반영 완료 (2026-08-22)**

- **블록 부트스트랩 신뢰구간을 화면에 직접 표기.** 252일 이동블록, 400회
  반복, 고정 시드. 다섯 구간 중 **네 곳은 중앙값의 부호조차 확정되지
  않는다.** 오늘 구간(80–100)은 중앙값 95% 구간이 −28.1~+20.9%,
  손실확률이 17.4~97.3%다. 이에 따라 헤드라인도 점 추정 주장을 버리고
  "그 자리가 무엇을 뜻하는지는 데이터가 답하지 못합니다"로 교체했다.
- **배당 누락 보정.** KRX에서 이미 받아두고 쓰지 않던 배당수익률을
  병합했다. 2001년 이후 코스피 평균 배당수익률은 **연 1.69%**이며,
  가격지수 기준인 이 페이지의 수치가 실제 투자 성과를 그만큼 낮게
  잡고 있다는 점을 별도 섹션으로 명시했다.

## 2차 검증 요청 — 1차 이후 추가된 것들 (아직 검증 안 받음)

1차 검증을 반영하는 과정에서 새 코드와 새 주장이 들어갔다. 이 부분은
아직 아무도 검토하지 않았다.

### 새로 추가된 주장
- 블록 부트스트랩 95% 구간: 다섯 구간 중 **네 곳은 중앙값의 부호조차
  확정되지 않는다** (`bucketBootstrap`, 252일 블록 · 400회 · 시드 20260822)
- 2001년 이후 코스피 **평균 배당수익률 연 1.69%** (`averageDividendYield`)
- 손익분기 승률의 손익비 민감도 **41~76%** (`payoffSensitivity`)
- 헤드라인이 신뢰구간에 따라 자동으로 바뀐다 (`headlineTwist`)

### 아직 아무도 짚지 않은 것
1. **부트스트랩 블록 길이 252일은 임의 선택이다.** 126일·504일로 바꾸면
   구간이 얼마나 달라지는가? 400회 반복은 충분한가?
2. **다중검정의 더 깊은 문제.** p값 보정만의 문제가 아니라, 후보 5개 중
   최고를 골라 **온도의 정의 자체를 정했다.** 이 선택 이후의 모든 통계가
   같은 데이터에서 나온다. 이걸 정직하게 보고하려면 어떻게 해야 하는가?
3. **1996년 이전 코스피 데이터 품질**, IMF 시기 지수 산출·구성 변경,
   KRX가 소급 수정한 값을 쓰고 있을 가능성.
4. **교차검증 장치 자체의 허점.** `scripts/verify_claims.py --api`의 허용
   오차(0.15~1.5)가 너무 느슨한가? 12개 항목이 충분한가? 통과하는데도
   틀릴 수 있는 경로가 있는가?
5. `mulberry32` 시드 고정이 부트스트랩 결과를 특정 방향으로 치우치게
   하지 않는가?

## 이 지표가 공개적으로 주장하는 것

1. 코스피가 다음 거래일에 오를 확률은 **51.9%** (1990~2026, 9,452회 관측)
2. 이 확률은 시장 온도가 몇 도이든 **51.3~56.3%** 안에 머문다
3. 기간을 늘리면 상승 비율이 **52 → 53 → 56 → 58 → 72 → 80%** 로 올라간다
4. 우리 지표의 워크포워드 방향 적중률은 **57.3%** — 같은 기간 '항상 상승' 기준선 **67.5%보다 10.2%p 낮다**
5. 겹치지 않는 표본 29개 기준 온도-1년수익률 상관은 **−0.298**, 통계적으로 유의하지 않다
6. 시장과 같은 손익 크기로 **매일** 매매해 본전을 맞추려면 **63.3%** 가 필요하다 (시장은 51.9%만 준다)
7. 매일 매매하면 연간 비용이 원금의 **70.6%** (단순합) / 자본의 **50.7%** (복리)

## 검증 대상 코드

| 파일 | 검증 포인트 |
|---|---|
| `lib/thermometer.ts` | 확장창 백분위, 워크포워드 채점, 구간 분포 |
| `lib/trading-cost.ts` | 손익분기 승률 공식 |
| `app/api/thermometer/route.ts` | 상승 확률, 잣대 산출 |
| `app/api/analysis/route.ts` | 후보 비교, 겹침 상관 |
| `scripts/fetch-history.mjs` | 네이버·FRED 수집·정렬 |
| `scripts/fetch-krx-valuation.py` | KRX PER/PBR 수집 |

## 우리가 이미 아는 약점 (여기부터 봐주세요)

### 1. 다중검정 미보정 — 확실한 결함
이격도·PER·PBR·CAPE·ERP 다섯 후보를 테스트해 **가장 성적이 좋은 이격도를 골랐다.**
다섯 중 최고를 고른 것 자체가 선택 편향이므로, 보고한 상관 −0.298의 실제 유의성은
더 나쁘다. 본페로니 또는 화이트의 리얼리티 체크로 보정하면 어떤 값이 되는가?

### 2. 손익분기 공식의 숨은 가정
```
p = (평균하락 + 비용) / (평균상승 + 평균하락)
```
이 식은 **예측력이 승률에만 영향을 주고 손익 크기 분포는 바꾸지 않는다**고 가정한다.
실제 트레이더는 손절·익절로 G/L 비율 자체를 바꿀 수 있다. 이 가정이 결론
(하루 단위 매매는 구조적으로 진다)을 얼마나 흔드는가?

### 3. 구간 분포의 겹침 (일부만 보정)
`buildBuckets`는 매일을 한 표본으로 세므로 1년 창이 364일씩 겹친다.
화면에는 실질 독립 표본(년)을 함께 표기했으나, **중앙값·손실확률 자체의 신뢰구간은
계산하지 않았다.** 블록 부트스트랩으로 구간을 구하면 얼마나 넓은가?

### 4. 확장창 백분위의 초기 구간
`WARMUP=750`, `MA_WINDOW=1250`. 앞의 약 2,000일이 소모되어 분석은 1996년부터
시작한다. 워밍업 길이를 바꾸면 결론이 뒤집히는가? (표본을 2012년부터로 좁혔을 때
구간별 단조성이 나타났다가 1996년부터로 넓히자 사라진 전례가 있다)

### 5. 생존 편향·지수 개편
코스피 지수 자체의 편입·제외 규칙 변경, 2001년 이전 PER/PBR 부재.
KRX가 소급 수정한 값을 쓰고 있을 가능성.

### 6. 거래비용 가정
개별주식 왕복 0.23%(증권거래세 0.15% + 수수료 0.03% + 슬리피지 0.05%),
ETF 왕복 0.08%. 2026년 기준으로 타당한가? 세율·수수료 최신값 확인 요청.

## 상시 교차검증 (CI 자동화)

`scripts/verify_claims.py`는 외부 검증자가 작성한 독립 구현이다.
TypeScript 구현을 일절 import하지 않고 `data/market-history.json`에서
온도 시계열을 처음부터 다시 만든다.

이제 매 push마다 GitHub Actions가 다음을 수행한다.

1. Next 앱을 빌드하고 실제로 띄운다
2. 파이썬으로 핵심 수치를 독립 재계산한다
3. 운영 API 응답과 항목별로 대조한다 (허용오차 포함)
4. 어긋나면 **CI가 실패**한다

구조적 점검도 포함한다. 예를 들어 1년 지평의 기준선으로 50%를
쓰고 있으면(과거에 저지른 오류) 실패한다.

```bash
python3 scripts/verify_claims.py                # 진단 리포트
python3 scripts/verify_claims.py --json         # 기계 판독용
python3 scripts/verify_claims.py --api <URL>    # 운영 API와 대조
```

현재 상태: **12/12 항목 일치**

## 검증 방법

```bash
git clone https://github.com/biguse/stock-radar && cd stock-radar
npm install && npm run dev
curl localhost:3000/api/thermometer   # 지표 전체
curl localhost:3000/api/analysis      # 후보 비교·겹침 상관
```
데이터는 `data/market-history.json` (9,453일), `data/krx-valuation.json` (6,324일).
수집을 다시 하려면 `scripts/fetch-history.mjs`(키 불필요),
`scripts/fetch-krx-valuation.py`(KRX 계정 필요).

## 요청 사항

- 위 7개 주장 각각을 **독립적으로 재현**해 숫자가 일치하는지
- 위 6개 약점 중 **결론을 뒤집을 수 있는 것**이 있는지
- 우리가 **놓친 편향**이 있는지 (특히 룩어헤드, 생존 편향, 데이터 스누핑)
- 시민 대상 공개 지표로서 **오독을 유발할 표현**이 남아 있는지

반박은 구체적으로, 가능하면 재현 코드와 함께 부탁드립니다.
